# Emitted next to auth stubs by mistake or local tsc — keep only *.ts in src/auth/
src/auth/**/*.js
src/auth/**/*.js.map
src/auth/**/*.mjs

# tsc emit from build:generated — keep only logging-adapter.ts in src/utils/
src/utils/**/*.js
src/utils/**/*.js.map

# tsc emit from build:generated — commit generated/**/*.ts only
generated/**/*.js
generated/**/*.js.map

tmp/
dist/
.cursor/plans/
