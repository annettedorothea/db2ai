# db2ai demos — tests

| File                        | What it covers                                                     |
| --------------------------- | ------------------------------------------------------------------ |
| `compile-generated.test.ts` | committed `generated/**/*.ts` compiles (`npm run build:generated`) |

Run from repo root: `npm test` (after `npm run install:demos` once). Release: `/test-all` in the demo workspace.

See [core2ai testing strategy](https://github.com/annettedorothea/core2ai/blob/main/docs/05-testing-strategy.md).
