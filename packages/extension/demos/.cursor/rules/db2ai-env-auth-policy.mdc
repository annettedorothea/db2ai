---
description: Bei db2ai keine Env-Dateien aendern; bei Auth-Fehler abbrechen, kein Credential-Workaround
alwaysApply: true
---

# Env-Dateien und Auth bei db2ai-MCPs

Diese Regel gilt, wenn die Nutzeranfrage mit `db2ai` beginnt.

## Env-Dateien nie anfassen

- **Nicht lesen, schreiben oder aendern:** `.env`, `.env.local`, `.env.*` und vergleichbare Secret-/Config-Dateien.
- **Keine Tokens, DB-URLs oder JWTs** in Env-Dateien oder Cursor-Settings schreiben — auch nicht „temporaer“ oder „damit der naechste MCP-Call klappt“.
- Der Nutzer verwaltet Credentials selbst. Du darfst hoechstens **allgemein** sagen, welche Variable betroffen ist (z. B. `MCP_AUTH_EXPECTED`, `PAGILA_POSTGRESQL_DATABASE_URL`) und dass der Nutzer sie bei Bedarf selbst setzen muss.

## Transport (HTTP vs. OAuth vs. stdio, siehe `mcp.json`)

Die meisten db2ai-Demo-MCPs nutzen **HTTP-URLs** in `.cursor/mcp.json`.

- **`sakila-mysql-stdio` (stdio):** `command`/`args` mit `--auth-env MCP_AUTH_EXPECTED`; DB-URL aus `SAKILA_MYSQL_DATABASE_URL` via `connectionEnv` (kein `--base-url-env`). Credential muss `MCP_AUTH_EXPECTED` matchen.
- **`sakila-mysql` / `pagila-postgresql` (HTTP passthrough):** `headers.x-api-token: demo` in Cursor; Validierung gegen `MCP_AUTH_EXPECTED` in `src/hooks/db2ai/*/verify*Credential.ts`.
- **`orders-postgresql` (HTTP oauth):** Cursor OAuth Sign-in; Validierung in `verifyOrdersPostgresqlCredential.ts` — kein Token in Env schreiben.
- **`sakila-mariadb`, `animals-sqlserver`, `plants-oracle` (HTTP public):** keine Auth-Header in `mcp.json`.

Auth-Logik liegt in `src/hooks/db2ai/**/verify*Credential.ts` — nicht als Host-CLI-Flags.

## Auth-Fehler: Fehler melden, nicht umgehen

- MCP-Server mit Auth (passthrough/OAuth) nutzen **ausschliesslich** das vom Host erwartete Credential/JWT bzw. den konfigurierten Header — optionale Tool-Argumente sind **kein** Ersatz.
- Schlaegt ein authentifizierter MCP-Call fehl (z. B. `401`, `403`, „customerId does not match JWT claim“, „Missing host credential“):
  - **Fehler woertlich** an den Nutzer melden,
  - **sofort stoppen** — keinen zweiten Versuch mit anderem `customerId`, anderem Credential oder anderer Identitaet,
  - kurz erklaeren, dass das **vorhandene Credential/JWT** nicht zur Anfrage passt und der Nutzer Env/Token selbst anpassen muss, wenn er andere Daten sehen will.
- **Kein Workaround:** kein CLI aus `generated/**`, kein direkter SQL-/HTTP-Call, kein Umgehen der Auth-Stubs unter `src/auth/**`.

## Kein Login- oder Token-Workaround

- db2ai-Demos haben **kein** separates `login`-Tool wie die api2ai Mock-API.
- **Nicht** versuchen, Identitaet per Tool-Aufruf zu wechseln, wenn Env auf eine andere Person zeigt.
- Wenn der Nutzer **explizit** eine andere Demo-Identitaet will (z. B. anderes JWT in `.env`): nur erklaeren, welche Variable er selbst setzen soll — **nicht** die Datei fuer ihn aendern.

## Beispiel (orders-postgresql)

**Anfrage:** „db2ai zeig bobs bestellungen an“ — OAuth-Token gehoert zu `alice`.

- ✅ Einmal `listCustomerOrders` mit `customerId: bob` aufrufen.
- ✅ Bei Mismatch-Fehler melden: Token ist `alice`, angefragt wurde `bob`; Nutzer soll per OAuth als `bob` anmelden (Cursor Sign-in auf `orders-postgresql`).
- ❌ `.env` lesen oder aendern.
- ❌ Erneut `listCustomerOrders` nach eigenmaechtigem Credential-Tausch.
- ❌ Pagila/Sakila-MCPs als Ersatz nutzen, um die Anfrage doch noch zu erfuellen.
