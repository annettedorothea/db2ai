# Test all db2ai MCP tools

Fuehre den Smoke-Test **aller** MCP-Tools **aller** Server aus `.cursor/mcp.json` aus.

1. Lies und befolge `.cursor/skills/db2ai-test-all-mcp/SKILL.md` vollstaendig.
2. Beginne die Ausfuehrung mit dem Praefix `db2ai /test:all` in deiner Antwort.
3. Schema-only: Parameter nur aus MCP-Tool-Descriptors, kein Repo-/DSL-Wissen.
4. Ergebnis: nur Zusammenfassungstabelle + Auffaelligkeiten — **kein** Audit-Abschnitt.
