{
    "name": "db2ai-examples",
    "private": true,
    "type": "module",
    "scripts": {
        "db:up": "docker compose up -d --wait",
        "db:down": "docker compose down",
        "db:reset": "docker compose down -v && npm run db:up",
        "db:psql": "docker compose exec pagila psql -U postgres -d pagila"
    },
    "dependencies": {
        "@modelcontextprotocol/sdk": "^1.29.0",
        "pg": "~8.16.0",
        "zod": "^4.4.3"
    }
}
